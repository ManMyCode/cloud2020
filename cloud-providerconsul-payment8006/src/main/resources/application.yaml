server:
  port: 8006
spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: localhost #consul注冊中心地址
      port: 8500
      discovery:
        service-name: ${spring.application.name}